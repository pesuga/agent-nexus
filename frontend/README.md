# Legacy Frontend (Reference)

This directory contains historical static HTML/JS prototypes used before `frontend-nextjs/` became the primary UI.

- Not the active production/frontend path.
- Kept for reference and design history.
- Prefer `frontend-nextjs/` for all new work.
